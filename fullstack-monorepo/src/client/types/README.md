# src/client/types/

<!-- TODO: describe your frontend-scoped type definitions. e.g.:
  "TypeScript type definitions scoped to the frontend. Types that are shared with the backend belong in `../../shared/` instead."
-->
